<?xml version="1.0" encoding="UTF-8"?>
<pageControls:BaseTopNavigationView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageControls="clr-namespace:AgeCal.Components"
             x:Class="AgeCal.Components.TopNavigationView">
    <ContentView.Content>
        <StackLayout Spacing="0" Orientation="Horizontal" AutomationId="TopNavBarContainer" HorizontalOptions="FillAndExpand" BackgroundColor="{StaticResource PrimaryCyan}"  VerticalOptions="CenterAndExpand" Margin="0" Padding="0">
            <ImageButton x:Name="BackIcon" AutomationId="BackIcon" Source="back.png" BackgroundColor="Transparent" HorizontalOptions="Start" IsVisible="False" Clicked="OnBackClick" WidthRequest="48" HeightRequest="48">
                
            </ImageButton>
            <StackLayout Spacing="0" Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <Label x:Name="TitleLabel" AutomationId="TitleLabel" FontSize="Large" HorizontalOptions="FillAndExpand" TextColor="{StaticResource PrimaryWhite}"
                       HorizontalTextAlignment="Center" VerticalOptions="FillAndExpand"
                       VerticalTextAlignment="Center" MaxLines="1" Margin="0" LineBreakMode="TailTruncation" />
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</pageControls:BaseTopNavigationView>